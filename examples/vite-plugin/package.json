{
  "name": "@hono-filebased-route/vite-plugin-example",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.4",
    "@hono-filebased-route/vite-plugin": "workspace:*"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.20.1",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}