{
  "name": "hono-filebased-route",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "test:basic": "cd examples/basic-example && bun dev"
  },
  "dependencies": {
    "hono": "^4.9.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.5.6",
    "typescript": "^5.0.0"
  },
  "packageManager": "bun@1.0.0"
}